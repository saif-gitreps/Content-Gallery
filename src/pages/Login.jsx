import { Login as LoginComp, ParentContainer } from "../components";

function Login() {
   return (
      <ParentContainer>
         <LoginComp />
      </ParentContainer>
   );
}

export default Login;
